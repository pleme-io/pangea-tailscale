# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Local pangea-core during fleet-wide development; the published gem
# expects pangea-core ~> 0.2 from rubygems once both are released.
gem "pangea-core", path: "../pangea-core"
